apiVersion: v1
kind: Service
metadata:
  name: gnc-postgres
  namespace: layer8
  labels:
    app: gnc-postgres
    app.kubernetes.io/name: gnc-postgres
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: gnc-postgres
