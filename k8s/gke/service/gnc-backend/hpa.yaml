apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: gnc-backend-hpa
  namespace: layer8
  labels:
    app: gnc-backend
    app.kubernetes.io/name: gnc-backend
    app.kubernetes.io/component: backend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: gnc-backend
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
