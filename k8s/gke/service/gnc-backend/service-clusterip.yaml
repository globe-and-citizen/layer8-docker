apiVersion: v1
kind: Service
metadata:
  name: gnc-backend
  namespace: layer8
  labels:
    app: gnc-backend
    app.kubernetes.io/name: gnc-backend
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: gnc-backend
