# IMPORTANT: THESE SECRETS ARE ONLY FOR DEVELOPMENT
apiVersion: v1
kind: Secret
metadata:
  name: postgres-standalone-secret
  namespace: layer8
  labels:
    app: postgres
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: database
type: Opaque
stringData:
  # PostgreSQL User Credentials
  POSTGRES_USER: "layer8development"
  POSTGRES_PASSWORD: "cRACtIeRChYsiNFinuMp"
  POSTGRES_DB: "development"
  
  # Alternative key names (for compatibility)
  postgres-user: "layer8development"
  postgres-password: "cRACtIeRChYsiNFinuMp"
  postgres-db: "development"
  
  # Connection String
  postgres-connection-string: "postgresql://layer8development:cRACtIeRChYsiNFinuMp@postgres.layer8.svc.cluster.local:5432/development"
  
  # PGDATA Path (if needed)
  PGDATA: "/var/lib/postgresql/data/pgdata"